services:
  docx-to-pdf:
    build: .
    container_name: docx-to-pdf
    ports:
      - "3333:3333"
    restart: unless-stopped
    healthcheck:
      test: ["CMD", "wget", "-q", "-O-", "http://localhost:3333/health"]
      interval: 30s
      timeout: 10s
      retries: 3
